---
import Base from "../layouts/Base.layout.astro";
import Workstation from "../components/Workstation.jsx";
---
<Base title="TerraSentry — Supply base">
  <!-- One island for the shell. Everything below the <head> is data-driven, so deferring
       hydration would only show an empty frame first. -->
  <Workstation client:load />
</Base>
