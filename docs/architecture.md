# TerraSentry — Architecture & Tech Stack

**Status:** Proposed for team review
**Date:** 2026-09-10
**Related:** [prd.md](./prd.md) · [milestones.md](./milestones.md)
**Scope:** MVP

This document decides the repo shape, the runtime architecture, and every framework around the
fixed choices: **Turborepo + pnpm** (JS/TS), **TanStack Start** (UI), **Effect** (TypeScript
runtime library), **Python + uv** (AI orchestration), and **AWS/SAP** integration. It also records
the alternatives that were considered and why they were rejected, so the decisions can be revisited
deliberately rather than by accident.

---

## 1. Executive summary

TerraSentry is a compliance product whose core is an **evidence-assembly agent**, not a CRUD app.
The architecture therefore puts the agent runtime and the data contracts at the center, and treats
the UI as a cockpit over a Python-owned API.

- **One backend**, in Python: FastAPI owns persistence, business logic, batch runs, and the agent
  runtime. This avoids a second TypeScript backend and the duplicate domain models that come with it.
- **One database**, PostgreSQL: suppliers, runs, per-step agent traces, evidence, verdicts, DDS
  documents, and cached source responses are relational data with a real audit-trail requirement.
- **Agents run on Strands Agents SDK** against Amazon Bedrock, using the SDK's multi-agent patterns
  (agents-as-tools and graph) to implement Supervisor, Specialists, and Independent Verifier. The
  same code can later be deployed to Bedrock AgentCore Runtime with minimal change.
- **Deterministic scoring stays in code**, not in the model. The LLM gathers evidence and writes
  narrative; `python/core/.../scoring.py` computes the number. This is what makes the verdict
  reproducible and answers "what if the AI hallucinates?".
- **The TypeScript side is Effect-first**: domain logic lives in `effect` services with `Schema` at
  trust boundaries, and the web app bridges Effect into React through a `ManagedRuntime` and
  TanStack Query. The FastAPI OpenAPI schema remains the contract source of truth.
- **Deployment is containers on AWS**, with CDK in TypeScript; AgentCore, SQS batch workers, and
  guardrails are explicit stretch items, not MVP dependencies.

---

## 2. Decision summary

| Layer | Decision | Package / technology |
| --- | --- | --- |
| Monorepo | Turborepo tasks + pnpm workspaces (JS) + uv workspaces (Python) | `turbo` 2.10, `pnpm` 12.3, `uv` |
| Runtime versions | Node >= 22, Python 3.13 | `.python-version`, `engines` |
| Web app | TanStack Start (React 19, Vite 8, file-based router) | `@tanstack/react-start` 1.168 |
| Web data/UI | TanStack Query, TanStack Table, Tailwind CSS 4, shadcn/ui, Lucide | generated by TanStack CLI |
| Web production server | Nitro Node server (`.output/server/index.mjs`) | `nitro` |
| TS runtime & domain layer | Effect 4 (RC): services, Layers, Schema, typed errors, HttpClient | `effect` (pnpm catalog) |
| Effect <-> React bridge | `ManagedRuntime` + TanStack Query loaders/hooks | `effect`, `@tanstack/react-query` |
| Maps | MapLibre GL JS + Amazon Location Service tiles | to add in the cockpit workstream |
| Backend | FastAPI + Uvicorn, SSE for live traces | `fastapi[standard]`, `sse-starlette` |
| ORM / migrations | SQLAlchemy 2.0 async + Alembic | `sqlalchemy[asyncio]`, `asyncpg`, `alembic` |
| Database | PostgreSQL 16+ (Neon for dev, RDS/Aurora Serverless v2 for AWS) | `postgresql+asyncpg://` |
| Source response cache | Redis with per-entry TTL (in-memory backend for tests) | `redis` (M1), facade in `python/integrations/.../cache.py` |
| Agent framework | Strands Agents SDK on Bedrock; multi-agent graph | `strands-agents`, `boto3` |
| Models | Role-based routing: one orchestrator/verifier model, one extraction model | `BedrockModel`; ids from `BEDROCK_MODEL_*` env |
| Geo / math | Shapely, pyproj | `shapely`, `pyproj` |
| HTTP sources | Async httpx with rate limiting, retry, and caching | `httpx`, `tenacity`, `aiolimiter` |
| API-to-web contract | FastAPI OpenAPI -> generated TS types, Effect Schema at runtime | `openapi-typescript`, `effect/Schema` |
| Infrastructure | AWS CDK in TypeScript | `aws-cdk-lib`, `constructs` |
| Lint / format | Biome (TS), Ruff (Python) | `@biomejs/biome`, `ruff` |
| Type checking | `tsc --noEmit` (TS), Pyright (Python) | `pyright` |
| Testing | Vitest + `@effect/vitest` (TS), pytest + respx (Python) | `@effect/vitest`, `pytest`, `pytest-asyncio`, `respx` |
| CI/CD | GitHub Actions, turbo cache, uv cache, CDK deploy | `.github/workflows` (to add) |

---

## 3. Repository layout

```
TerraSentry/
├── apps/
│   ├── api/                          Python service (uv workspace member)
│   │   ├── Dockerfile                container image for AWS (runs migrations then uvicorn)
│   │   ├── package.json              Turbo/PNPM shim so `pnpm dev` and `pnpm test` include Python
│   │   ├── pyproject.toml            terrasentry-api
│   │   ├── alembic.ini + alembic/    async migration environment (M4)
│   │   ├── src/terrasentry_api/
│   │   │   ├── main.py               FastAPI app factory + lifespan + error mapping
│   │   │   ├── config.py             pydantic-settings
│   │   │   ├── db.py                 async engine + session factory
│   │   │   ├── models.py             SQLAlchemy audit schema (M4)
│   │   │   ├── store.py              deep persistence module for runs/steps/evidence/verdicts/DDS
│   │   │   ├── runner.py             background run manager + SSE broadcaster (M4)
│   │   │   ├── services.py           lifespan-owned app services + dependencies (M4)
│   │   │   ├── schemas.py            API request/response models (M4)
│   │   │   ├── seed_loader.py        idempotent seed upsert (M4)
│   │   │   ├── mock_sap.py           in-memory SAP stub for the M7 gateway
│   │   │   ├── export_openapi.py     dumps OpenAPI for the TS client
│   │   │   └── routers/              health, suppliers, runs, batch, dds, mock_sap
│   │   └── tests/                    TestClient suites on SQLite + respx
│   └── web/                          TanStack Start cockpit (pnpm workspace member)
│       ├── Dockerfile                Nitro production image
│       ├── package.json              @terrasentry/web
│       └── src/
│           ├── features/             query modules bridging Effect into TanStack Query
│           ├── lib/api.ts            runApi: Effect -> Promise bridge for loaders/hooks
│           ├── lib/runtime.ts        ManagedRuntime wired with the ApiClient layer
│           ├── routes/               file-based routes (index, suppliers, runs, batch)
│           ├── integrations/         TanStack Query provider + devtools
│           ├── router.tsx
│           └── styles.css
├── python/
│   ├── core/                         terrasentry-core (uv member)
│   │   └── src/terrasentry_core/
│   │       ├── agents/               supervisor, specialists, verifier, nodes, orchestrator, preflight
│   │       ├── domain/               models, enums, verdict types
│   │       ├── seed/                 (M1) deterministic synthetic seed generator
│   │       ├── reference/            (M1) parity baseline over the same GFW/FIRMS tools
│   │       ├── tools/                (M3) shared source/seed tools + run trace
│   │       ├── evidence.py           citation ledger (M2)
│   │       ├── scoring.py            deterministic rubric (M2)
│   │       ├── dds.py                EUDR/TRACES V3-aligned payload builder (M2)
│   │       └── assessment/           M2 scoring pipeline + CLI over reference runs
│   └── integrations/                 terrasentry-integrations (uv member)
│       └── src/terrasentry_integrations/
│           ├── sources/              GFW/Hansen, NASA FIRMS clients (+ shared http.py)
│           ├── sap/                  SapGateway protocol + sandbox/stub clients
│           ├── cache.py              Redis/memory cache keyed by source + geometry + window
│           ├── preflight.py          live credential/latency/quota probe
│           └── settings.py           pydantic-settings for sources and cache
├── packages/
│   ├── api-client/                   @terrasentry/api-client
│   │   └── src/
│   │       ├── client.ts             ApiClient service (Effect HttpClient + retry)
│   │       ├── errors.ts             Schema.TaggedError types
│   │       ├── runtime.ts            ManagedRuntime factory
│   │       ├── schema.d.ts           generated OpenAPI types (compile-time contract)
│   │       ├── schemas.ts            Effect Schema decoders (runtime contract)
│   │       └── schemas.test.ts       @effect/vitest suite
│   └── config/                       @terrasentry/config (shared tsconfig base)
├── infra/
│   └── cdk/                          @terrasentry/infra (AWS CDK app)
│       ├── bin/terrasentry.ts
│       └── lib/terrasentry-stack.ts  split into stacks as resources land
├── data/
│   ├── seed/                         synthetic 50-record batch + legality dataset
│   └── fixtures/                     cached real API responses for rehearsals/demo
├── docs/
│   ├── prd.md
│   └── architecture.md               this file
├── pyproject.toml                    uv virtual workspace root (no [project] table)
├── pnpm-workspace.yaml               JS workspace, version catalog, allowBuilds, release-age policy
├── turbo.json                        JS task graph
├── package.json                      root scripts bridging pnpm and uv
├── biome.json                        shared TS lint/format for packages/ and infra/
├── AGENTS.md                         agent conventions incl. the Effect reading requirement
└── .env.example                      every key the pipeline needs
```

### Why two workspace roots

- pnpm globs `apps/*`, `infra/*`, `packages/*`. `apps/api` carries a minimal `package.json` shim so
  Turborepo can schedule its `dev`, `lint`, `test`, and `typecheck` alongside the web app. Those
  scripts shell into `uv`.
- `pyproject.toml` at the repo root is a **virtual uv workspace**: no `[project]` table, members are
  `apps/api` and `python/*`. One shared `.venv` and one `uv.lock`. Dev tools (pytest, Ruff, Pyright)
  live in the root `[dependency-groups].dev`, so `uv run pytest` at the repo root covers every Python
  package.

---

## 4. Runtime architecture

```
                    Browser (TanStack Start cockpit)
                     |  Effect services via @terrasentry/api-client
                     |  ManagedRuntime -> route loaders / TanStack Query
                     |  SSE for live agent trace
                     v
        +---------------------------------------------------+
        |  apps/api  ·  FastAPI                             |
        |  REST + SSE + batch worker (asyncio)              |
        |  SQLAlchemy async -> PostgreSQL                   |
        +---------+---------------------------+-------------+
                  |                           |
                  v                           v
        +--------------------+      +------------------------+
        | terrasentry-core   |      | terrasentry-           |
        | Supervisor Agent   |      | integrations           |
        |  ├ Geospatial      |      |  ├ GFW / Hansen GFC    |
        |  ├ Thermal (FIRMS) |----->|  ├ NASA FIRMS          |
        |  ├ Legality (mock) |      |  ├ SAP BTP / API Hub   |
        |  └ Verifier        |      |  └ cache + rate limits |
        | rubric, evidence,  |      +-----------+------------+
        | DDS writer         |                  |
        +---------+----------+                  v
                  |                    +-------------------+
                  v                    +  external APIs    |
        +--------------------+         +-------------------+
        | Amazon Bedrock     |
        | configured models  |
        +--------------------+
                  |
                  v  (stretch)
        +--------------------+
        | Bedrock AgentCore  |
        | Runtime + Memory   |
        +--------------------+
```

**Request flow (live scenario):** `POST /runs` creates a run and starts the agent task ->
supervisor calls specialist tools -> each tool result is written to the evidence ledger and the run
trace in Postgres -> the verifier independently cross-checks -> the deterministic rubric scores ->
DDS payload is generated -> the run ends `complete` or `awaiting_review`. The UI subscribes to
`GET /runs/{id}/stream` (SSE) for the step-by-step trace, then fetches the final dossier.

**Agent graph (Strands):**

```
supervisor (orchestrator model, agents-as-tools)
   ├── geospatial_analyst (extraction model) ── get_tree_cover_loss(polygon_id) ─┐
   ├── thermal_analyst    (extraction model) ── get_fire_hotspots(polygon_id)    ├─ shared tools/
   └── legality_analyst   (extraction model) ── get_legality_record/consignment  ┘  (ids only)
        │
        v
   assessor (deterministic)  -> candidate Assessment + ledger + DDS draft
        │
        v
   verifier (orchestrator model + deterministic checks) -> re-derives metrics,
        │                                                  validates citations,
        │                                                  challenges the summary
        │ accepted edge only
        v
   dds_writer (deterministic) -> releases the DDS, or withholds it for
                                 `awaiting_review` (human approve/override)
```

Strands' **agents-as-tools** pattern gives the supervisor explicit delegation, and its **Graph**
builder gives the verify-before-write edge: `dds_writer` only runs when the verifier's
`VerificationReport.accepted` is true. The verifier is a separate agent with a different prompt and
only read tools over the evidence ledger, so "verification is a real step" is structurally true,
not decorative. Ambiguous verdicts keep the DDS in `pending_assessment` until a human decision is
recorded through the M2 run-state machine. Everything the model sees comes from ids-only tools; the
rubric always computes the score.

---

## 5. Stack decisions and rejected alternatives

### 5.1 Monorepo: Turborepo + pnpm + uv

**Decision:** Turborepo for the JS/TS task graph, pnpm workspaces for dependencies, uv workspaces
for Python. Root `package.json` scripts are the single entrypoint (`pnpm dev`, `pnpm lint`,
`pnpm typecheck`, `pnpm test`, `pnpm gen:api`) and bridge into Python through the `apps/api` shim.

**Why:** Turborepo gives caching and dependency-aware task ordering for the TS packages; pnpm 12 is
fast and pins shared versions through its catalog (`effect` today, more later); uv gives one
lockfile, one virtualenv, and reproducible
`pyproject.toml` resolution for the Python side. Each ecosystem uses its native tool — no
cross-language plugin hacks.

**Rejected:** Nx (heavier, most value is in TS-only plugin ecosystems); Poetry/pip-tools
(slower, uv workspaces cover the same ground with less config); a single "just"-style task runner
(loses remote-cache and dependency graph behavior).

### 5.2 Frontend: TanStack Start

**Decision:** TanStack Start with the file-based router, TanStack Query for server state, TanStack
Table for the batch/supplier grid, Tailwind CSS 4 + shadcn/ui for the cockpit, Nitro for the
production Node server.

**Why:** the PRD explicitly protects time spent on UI ("a clean table + status view is
sufficient"). TanStack Start is already wired to the same Query ecosystem used for API calls, has
typed file routes, and produces a standalone container build verified locally
(`apps/web/.output/server/index.mjs`). No bespoke design system is needed.

**Rejected:** Next.js (chosen stack was specified; also heavier build and different data-fetching
model); SPA-only Vite React (loses SSR for the dossier pages and adds client-side routing boilerplate
for no benefit at this scale); Remix/React Router framework mode (comparable, but duplicates the
TanStack Query story the team already committed to).

**Effect integration:** components never call `fetch` directly. Data access is an Effect service in
`@terrasentry/api-client`; `apps/web/src/lib/runtime.ts` builds the `ManagedRuntime`,
`apps/web/src/lib/api.ts` bridges an effect to a promise (`runApi`), and feature modules (for
example `apps/web/src/features/health/queries.ts`) expose TanStack Query options on top. Route
loaders pre-fetch with `context.queryClient.ensureQueryData` so SSR ships dehydrated data. See 5.12
for the full rule set.

**Design system:** Tailwind CSS 4 plus shadcn/ui on Base UI primitives (Mira style, pinned as
`style: base-mira` in `apps/web/components.json`). Every design token from `DESIGN.md` lives in a
single file, `apps/web/src/styles.css`: raw palette, type ladder, radii, flattened weights, and the
semantic aliases that components consume. The UI is dark-canvas only; there is no light mode.
Fonts are self-hosted (Inter Variable, Geist Mono Variable).

### 5.3 Backend: FastAPI (Python)

**Decision:** FastAPI as the only backend. It serves REST, SSE, and the batch worker, and it owns
the database.

**Why:** the agents are Python, so making the backend Python removes a network hop and avoids
maintaining domain models twice. FastAPI's OpenAPI output becomes the contract for the TS client.
Background work runs as asyncio tasks with a bounded semaphore, which is sufficient for a 50-record
batch and needs no extra infrastructure (Celery/Redis) on demo day.

**Rejected:** Hono/NestJS backend in TypeScript (would need the domain model, DB access, and job
scheduling duplicated across languages); GraphQL (no client-driven query complexity to justify it);
gRPC (browsers need a proxy; REST + SSE is simpler and demo-visible).

### 5.4 Database: PostgreSQL + SQLAlchemy 2.0

**Decision:** PostgreSQL with SQLAlchemy 2.0 async + asyncpg + Alembic. Dev on Neon free tier or a
local/Docker Postgres; AWS on RDS or Aurora Serverless v2.

**Why:** the audit trail is inherently relational and queryable: suppliers -> parcels -> runs ->
steps -> evidence -> verdicts -> DDS. JSONB handles flexible agent traces and raw payload snapshots
without a second store. Aggregate throughput queries for the batch are trivial SQL.

**Rejected:** DynamoDB (single-table design overhead for trace/aggregate access patterns; local dev
needs Java tooling; the PRD's DynamoDB mention is only the SAP-less fallback for a status write);
SQLite as the deployed store (no concurrency story for batch runs; keep it only as an optional
unit-test backend); Prisma/Drizzle (the DB is owned by Python; a TS ORM would be a second writer).

### 5.5 Agent orchestration: Strands Agents SDK

**Decision:** Strands Agents SDK with Bedrock models, using agents-as-tools for the
Supervisor -> Specialists structure and a graph for verify-before-write. Optional extra
`terrasentry-core[agentcore]` adds `bedrock-agentcore` for Runtime and Memory deployment.

**Why:** Strands is AWS's first-party agent SDK, so the Bedrock path, tracing, and AgentCore story
are native rather than adapted. It supports exactly the two patterns the PRD needs (delegation and
an ordered verifier step) in code, and it can run locally against Bedrock during development.

**Rejected:** LangGraph (strong alternative, especially for durable checkpointing and HITL
interrupts; rejected as the default because it is a general graph engine rather than an
AWS-integrated agent SDK. Revisit if resumable long-running approvals become a hard requirement);
CrewAI/AutoGen (less control over the verification step, more prompt magic to explain on stage);
direct Bedrock Converse tool-use loop (the existing RIMBA prototype did this; it works, but
re-implements delegation, tracing, and agent packaging that Strands already provides. Keep the
prototype as the reference implementation for parity tests).

### 5.6 AI models and routing

| Job | Role | Rationale |
| --- | --- | --- |
| Supervisor decisions, verification, final synthesis | `orchestrator` model | low volume, high judgement |
| Extraction/normalisation (names, legal fields, units) | `extraction` model | high volume, low judgement, cheap |

The concrete Bedrock model for each role is configuration, not code:
`BEDROCK_MODEL_ORCHESTRATOR` and `BEDROCK_MODEL_EXTRACTION` are required for the live agent path
(`--model bedrock`) and accept plain model IDs or cross-region inference profiles, so changing the
model is an `.env` edit. Verify model access and current IDs in the target account before build day
(PRD risk table).

The live client follows Bedrock best practice: adaptive retries (`AGENT_RETRY_MAX_ATTEMPTS`,
default 5) with explicit connect (10s) and read (`AGENT_READ_TIMEOUT_SECONDS`, default 120s)
timeouts, and an explicit `max_tokens` so Bedrock does not reserve the model maximum. Prompt caching
is opt-in (`BEDROCK_PROMPT_CACHE=off|auto|anthropic`) and defaults off because the roles' static
prefixes are below the model minimums (Claude Sonnet 4.5: 1,024 tokens, Haiku 4.5: 4,096), where
cache points are silently ignored; `anthropic` is the explicit mode for opaque ARN inference
profiles. `python -m terrasentry_core.agents.preflight` pings both roles through the same Strands path
and maps AWS failures to fixes, which is the §3 model-access gate check.

**Non-negotiable:** the model never produces the risk score. `scoring.py` computes it from structured
tool outputs; the model writes the explanation. Every claim in the dossier must pass through the
evidence ledger with a source, artifact, and retrieval timestamp.

### 5.7 SAP integration

**Decision:** define `SapGateway` as a Python protocol (already in
`python/integrations/.../sap/__init__.py`) with three implementations selected by `SAP_MODE`:

1. `sandbox` — real calls to SAP API Business Hub / BTP trial via httpx and OData payloads.
2. `stub` — a FastAPI router that serves API Hub-accurate payload shapes (PRD Option 2).
3. `live` — reserved for a tenant with credentials.

**Why:** the PRD's decision tree is about access, not code. A single protocol keeps the agent code
identical whether the action is real, sandbox, or stub, and the demo can state the boundary
honestly.

**Rejected:** SAP Cloud SDK for JavaScript/Java (`@sap-cloud-sdk/http-client`, `odata-v4`) — solid
tooling, but it would force a TypeScript integration service for one or two endpoints. Plain HTTP +
Pydantic models generated from the published OData metadata is enough at this scope.

### 5.8 Contracts: OpenAPI -> TypeScript

**Decision:** the FastAPI app is the source of truth. `pnpm gen:api` imports the app, writes
`packages/api-client/openapi.json`, and runs `openapi-typescript` to produce
`packages/api-client/src/schema.d.ts` (the compile-time contract: paths, params, response shapes).
At runtime, `packages/api-client/src/client.ts` calls the API with the Effect `HttpClient`, retries
transient failures, and decodes responses with Effect `Schema` (`schemas.ts`), so untrusted
payloads are validated before they reach the UI.

**Why:** two complementary guarantees. An API change that removes or renames a field fails `tsc`
against the generated types, and a malformed payload fails `Schema` decoding at the boundary with a
typed `ApiClientError` instead of leaking into components. Generated output can be diffed in CI.
Planned: a CI step that regenerates `schema.d.ts` plus a type-level assertion that each
`Schema.Type` stays assignable to its generated counterpart.

**Rejected:** openapi-fetch (a thin typed fetch wrapper; Effect `HttpClient` provides retry,
timeouts, spans, and typed errors in the same composition); tRPC (impossible across
Python/TypeScript); hand-written shared types (drift); Zod-first contracts (source of truth would
move to TS, away from the Python domain; Effect `Schema` already covers parsing and encoding).

### 5.9 AWS integration

| Concern | Service / SDK |
| --- | --- |
| Model inference | Amazon Bedrock (Strands `BedrockModel`, boto3) |
| Agent deployment (stretch) | Bedrock AgentCore Runtime + Memory |
| Object storage (raw payloads, raster chips, DDS artifacts) | S3 via boto3 |
| Database | RDS / Aurora Serverless v2 PostgreSQL |
| Batch scale-out (stretch) | SQS + worker container; EventBridge Scheduler for re-screening |
| Maps | Amazon Location Service tiles rendered by MapLibre GL |
| Secrets | Secrets Manager (deployed) / `.env` (local, gitignored) |
| Observability | CloudWatch Logs + OpenTelemetry (ADOT) / X-Ray |
| IaC | AWS CDK (TypeScript) in `infra/cdk` |

**Rejected:** Terraform (adds a second language and state management for a small team; CDK keeps
infra in TypeScript next to the app); SST v3 (great TanStack Start deployment DX, but AgentCore and
finer IAM control favor CDK; SST can be evaluated later for the web tier); Vercel/Amplify for the
web app (splits the deployment surface across vendors; a container keeps web and API behind one
CloudFront distribution and path routing).

### 5.10 Rate limits, caching, and source reliability

PRD section 5 makes this a build-blocking concern, so it is designed in from the start:

- `httpx.AsyncClient` with `tenacity` exponential backoff + jitter and an `aiolimiter` per-source
  concurrency cap.
- Persistent Redis cache keyed by `(source, geometry_hash, date_window)` with a per-entry TTL
  (`ts:cache:v1:*`). Each entry stores the request description plus a normalized snapshot of the
  source-derived result (including `fetched_at`), so cached re-runs make zero external calls and
  remain auditable. Postgres stays the audit/evidence store; Redis is only the hot response cache.
  An in-memory backend serves tests and no-Docker smoke runs. Fixtures are deliberately not
  committed — warm the cache with a live run, then use `--offline` for rehearsals.
- A preflight script runs 5-10 dummy lookups before committing engineering time to the 50-record
  batch, per the PRD action item, and reports observed latency and quota behaviour.

### 5.11 Testing and quality

| Scope | Tooling | What it covers |
| --- | --- | --- |
| Python unit/integration | pytest, pytest-asyncio, respx | geometry, rubric determinism, evidence ledger, DDS shape, API routes, source clients with mocked HTTP |
| TS unit/component | Vitest + `@effect/vitest` (`it.effect`) | Effect services and Schema decoders (started in `packages/api-client/src/schemas.test.ts`); component tests to add |
| End-to-end (optional) | Playwright | one scripted live-demo path |
| Contract (optional) | Schemathesis against OpenAPI | catches API regressions early |
| Lint/format | Biome (TS), Ruff (Python) | enforced in `pnpm check` |
| Types | `tsc --noEmit`, Pyright | enforced in `pnpm check` |

CI plan: GitHub Actions jobs for JS (corepack pnpm install -> `turbo run lint typecheck test build`)
and Python (`uv sync --locked` -> `uv run ruff check`, `uv run pyright`, `uv run pytest`), with
pnpm and uv caches. Deploy job runs `cdk deploy` behind an environment approval.

### 5.12 Effect: the TypeScript domain layer

**Decision:** `effect` (4.0.0-rc, pinned in the pnpm catalog; installed as a root dev dependency
per the repo skill so `node_modules/effect` is always available to agents and editors) is the
standard library for all TypeScript logic outside React rendering.

Rules for TS work:

1. Write effects with `Effect.gen` / `Effect.fn("name")`; do not return raw `Effect.gen` from a
   named function.
2. Every external dependency is a `Context.Service` with a `Layer` (`ApiClient` is the reference
   implementation).
3. All untrusted input crosses a `Schema` decoder; use `Predicate` for runtime checks, never
   hand-rolled guards.
4. Failures live in the error channel as `Schema.TaggedError` types and are handled with
   `Effect.catchTag` / `Effect.catchTags`.
5. Use `Stream` for SSE/event flows, `Schedule` for retry/polling, and `DateTime` instead of `Date`.
6. React components do not call `fetch`; they consume effects through the `ManagedRuntime` in
   `apps/web/src/lib/runtime.ts`.

**Why:** typed errors, automatic retries, tracing spans, dependency injection, and runtime
validation are exactly the concerns of a compliance UI reading a Python API. The same code also
works unchanged in server contexts (Nitro SSR today, a future worker or server function).

**Rejected:** `@effect/atom-react` as the default React state layer — it is on the 4.0 beta line
while `effect` is on RC, and TanStack Query already provides the SSR dehydration the cockpit needs.
Revisit only if client state outgrows server-cache patterns. Also rejected: `effect/unstable/sql`
and `effect/unstable/httpapi` for this project, because PostgreSQL and the HTTP API are owned by
Python (section 5.3).

---

## 6. Cross-cutting design rules

1. **The model never invents numbers.** Deterministic rubric in code; model explains.
2. **Every claim carries a citation.** Sources, artifacts, and retrieval timestamps live in the
   evidence ledger; the verifier rejects uncited claims.
3. **No-Go is never automatic.** Runs end in `awaiting_review` for the ambiguous branch; a human
   decision is recorded before any DDS is finalized.
4. **Simulated data is labelled as simulated.** Legality/entity fields are synthetic and surfaced as
   such in the UI and dossier.
5. **The reference pipeline and the agent share the same tools.** A hardcoded pipeline remains the
   parity test for the agent, as in the previous prototype.

### Run state machine

```
queued -> running -> complete
             |  \
             |   -> needs_more_data (retryable)
             v
        awaiting_review -> (approve | override) -> complete
             |
             v
          failed -> retry / manual
```

### Batch execution (PRD section 4)

`POST /batch-runs` creates a run plus 50 record rows (30 compliant / 12 high-risk / 8 ambiguous,
generated around real forest coordinates in Sumatra, Kalimantan, and Riau). An in-process asyncio
worker processes records with bounded concurrency and per-source limiters, persisting per-record
elapsed time and status. `GET /batch-runs/{id}/stream` emits progress over SSE; the final summary
reports wall-clock time, average per record, and the pass/fail/ambiguous breakdown. When moving to
AWS, the same worker runs as a container consuming SQS — the API contract does not change.

### Live agent trace

The demo needs the reasoning visible. The API persists every supervisor turn, tool call, and tool
result as run steps, and streams them to the cockpit. OpenTelemetry/AgentCore observability is for
operators; the Postgres run trace is the demo's source of truth because it is deterministic and
replayable from `data/fixtures`.

### Secrets and configuration

Single `.env` at the repo root for local development (gitignored, `.env.example` committed). In AWS,
values come from Secrets Manager and task IAM roles. `pydantic-settings` validates the API side; the
web app only receives `VITE_*` values at build time.

---

## 7. Deployment topology

```
                         CloudFront
                             |
                    ALB (path-based routing)
               /api/*  |                |  /*
                       v                v
              ECS Fargate / App Runner  ECS Fargate / App Runner
              apps/api (FastAPI)        apps/web (Nitro Node)
                       |                        |
                       +-----------+------------+
                                   |
                      RDS PostgreSQL / Secrets Manager / S3
                                   |
                        Amazon Bedrock / (stretch) AgentCore
```

- **MVP:** two containers, ALB path routing (`/api/*` -> API, else web), RDS Postgres in a private
  subnet, S3 for artifacts, CloudWatch logs. CDK app in `infra/cdk` grows one stack per concern
  (`Network`, `Data`, `Api`, `Web`, `Observability`).
- **Stretch:** deploy the Strands agent to AgentCore Runtime (`bedrock-agentcore-control`
  `create_agent_runtime` with an ECR image), use AgentCore Memory for per-supplier history, and
  AgentCore Observability for traces. This is additive because the agent code already runs on
  Strands.
- **Local:** `pnpm dev` runs web (port 3000) and API (port 8000) with CORS and the Effect API
  client. Web calls `VITE_API_BASE_URL` directly; production uses the same path scheme through
  CloudFront.

---

## 8. Real vs simulated, mapped to code

| PRD component | Reality | Code location |
| --- | --- | --- |
| Geospatial Analyst (GFW/Hansen) | Real API | `python/integrations/.../sources/` + `python/core/.../agents/` |
| Thermal & Anomaly (NASA FIRMS) | Real API | same, separate source client + specialist |
| Independent Verifier | Real (LLM cross-check) | `python/core/.../agents/verifier.py` |
| Legality & Entity | Simulated, labelled | `data/seed/` + legality specialist |
| Supervisor | Real (Strands graph) | `python/core/.../agents/supervisor.py` |
| Compliance & DDS Writer | Real (JSON/XML) | `python/core/.../dds.py` |
| SAP actions | Sandbox or stub, disclosed | `python/integrations/.../sap/` with `SAP_MODE` |
| 2 live scenarios | Real data, full trace | seeded polygons in `data/seed/` + fixtures cache |
| 50-record batch | Real GFW/FIRMS calls per record, synthetic legality | `apps/api` batch worker + `data/seed/` |

---

## 9. Suggested build order (de-risking first)

0. **Day 1, before code:** check SAP API Business Hub / BTP trial access; register the NASA FIRMS
   key; request Bedrock model access; verify the GFW key. These have external lead times (PRD
   sections 5 and 6).
1. **Integrations + cache:** GFW and FIRMS clients with retry, limiter, and cache; test with 5-10
   records. This is the schedule-critical path.
2. **Deterministic core:** domain models, evidence ledger, rubric, DDS builder — testable with no
   AWS account.
3. **Agents:** Strands supervisor/specialists/verifier wired to the same tools; parity against the
   reference pipeline.
4. **API:** Postgres models + Alembic, run/step/batch endpoints, SSE.
5. **Cockpit:** Effect runtime + query modules, supplier table, run detail with live trace, batch
   summary, map.
6. **Batch:** 50-record generation around real forest coordinates; measure throughput.
7. **SAP action:** sandbox if access landed, otherwise the schema-accurate stub.
8. **Rehearsal:** pre-fetch the demo cache, run the batch end-to-end, then deploy to AWS.

---

## 10. Getting started

Prerequisites: Node >= 22 (corepack enabled), pnpm via corepack, `uv`, and Docker (for the local
Postgres + Redis from `pnpm db:up`) or your own Postgres connection string in `DATABASE_URL`
(Neon free tier is the zero-install option; set `CACHE_BACKEND=memory` for a no-Docker smoke run).

```bash
corepack enable
pnpm install
uv sync
cp .env.example .env

pnpm db:up      # Postgres + Redis containers
pnpm db:upgrade # apply the Alembic audit schema
pnpm dev        # web on :3000 and API on :8000
pnpm check      # Biome + Ruff + tsc + Pyright
pnpm test       # Vitest + @effect/vitest (api-client) + pytest
pnpm gen:api    # regenerate packages/api-client from the FastAPI schema

uv run python -m terrasentry_integrations.preflight   # live source probe (needs API keys)
uv run python -m terrasentry_core.reference            # live run, then cached
uv run python -m terrasentry_core.reference --offline  # zero external calls
uv run python -m terrasentry_core.agents --record REC-001 --model scripted
uv run python -m terrasentry_core.agents.preflight              # live Bedrock gate check (both roles)
uv run python -m terrasentry_core.agents --scenario high_risk_live --model bedrock  # live agents
```

Before writing Effect code, read `node_modules/effect/AGENTS.md` (required by the repo `AGENTS.md`).
The Effect version is pinned once in the pnpm catalog in `pnpm-workspace.yaml`.

Useful filters:

```bash
pnpm --filter @terrasentry/web dev
pnpm --filter @terrasentry/api dev
pnpm exec turbo run typecheck --filter=@terrasentry/web
```

---

## 11. Current scaffold status (2026-09-10)

Verified locally:

- `pnpm install` across all six workspace projects (`pnpm-lock.yaml`); `uv sync --all-packages`
  generated `uv.lock`, and `uv run ruff check .`, `uv run pyright`, and `uv run pytest` are green.
- `effect@4.0.0-rc.113` pinned in the pnpm catalog and consumed by `@terrasentry/api-client` and
  `@terrasentry/web`.
- `pnpm check`, `pnpm test`, and `pnpm build` are green; the `@effect/vitest` suite in `api-client`
  passes (2 tests) plus the API health test.
- `pnpm gen:api` produces `packages/api-client/openapi.json` and a real `src/schema.d.ts` for
  `/health`; regeneration is diff-clean.
- `@terrasentry/web` production build and Nitro server returns HTTP 200; `pnpm dev` starts both
  apps and SSR renders the live API status ("Online") through the Effect client.
- The UI is Tailwind v4 + shadcn/ui (Base UI, Mira style) with all `DESIGN.md` tokens centralized in
  `apps/web/src/styles.css` and fonts self-hosted (Inter Variable, Geist Mono Variable).
- `compose.yaml` validates (Postgres + API + web) and `.github/workflows/ci.yml` defines js, python,
  contract, and docker jobs.
- `cdk synth` runs via `tsx` and emits `infra/cdk/cdk.out`; one TypeScript version is pinned in the
  pnpm catalog and `pnpm peers check` is clean.

Pending (needs credentials, Docker egress, or later workstreams):

- Local Docker image builds: container egress is blocked in the scaffold environment, so both images
  are built by the CI `docker` job instead; the full `docker compose up` smoke test is still pending.
- CDK stacks beyond the empty app shell.

M1 landed (2026-09-11): GFW/Hansen and NASA FIRMS clients with per-source retry, rate limiting, and
a Redis response cache; the GFW async batch path; deterministic seed data (8 demo polygons plus the
30/12/8 batch with synthetic legality); the reference pipeline (`python -m terrasentry_core.reference`);
the preflight probe (`python -m terrasentry_integrations.preflight`); and the setup runbooks under
`docs/setup/`. The live probe and reference run are pending API keys (see `docs/milestones.md` §3).

M2 landed (2026-09-11): the deterministic core in `domain/`, `evidence.py`, `scoring.py`, and
`dds.py`, plus the `assessment/` pipeline and CLI. The rubric is config-driven (`RubricConfig`, no
clock/network/model), the ledger uses content-hash ids and source timestamps, and the DDS builder
mirrors the public EUDR Information System V3 operator API (SOAP `SubmitDdsRequest` namespaces and
the GeoJSON >4 ha polygon rule) in both JSON and XML. `python -m terrasentry_core.assessment` scores
an M1 reference run and writes per-record DDS/evidence artifacts plus a `summary.json` breakdown that
M6 uses to calibrate thresholds against live data. 85 pytest tests green; the committed DDS golden
fixture (`python/core/tests/fixtures/dds_reference.{json,xml}`) locks the shape.

M3 landed (2026-09-11): the agent stack in `tools/` and `agents/`. `tools/sources.py` is the single
GFW/FIRMS fetch now used by both `reference/` and the agent tools; `tools/agent_tools.py` exposes
ids-only Strands tools plus read-only verifier tools; `agents/` adds Bedrock model routing
(`ModelBundle`, `AgentSettings`), a deterministic `ScriptedModel`/`AutopilotResponder` for offline
runs, the agents-as-tools supervisor, specialists, a verifier node that re-derives metrics and
validates citations before an LLM review, and `RunOrchestrator` with the graph verify-before-write
edge and the `awaiting_review`/resume HITL seam. `python -m terrasentry_core.agents` runs a record
end-to-end (exit 0 complete / 2 awaiting review / 1 failed). Parity is asserted per record against
the reference pipeline (identical `Assessment`, fingerprint, and DDS) on shared mock data; verifier
catches and HITL are covered. Since parity compares live against cached runs, an M2 defect was
fixed: `cached` is no longer part of the hashed evidence artifact (`EvidenceEntry.cached` outside
the hash), so cached re-runs are fingerprint-identical. 108 Python tests green; the live Bedrock
path waits on the §3 model-access gate and the `BEDROCK_MODEL_*` ids chosen for the environment.

M4 landed (2026-09-11): the API and persistence layer. `apps/api/src/terrasentry_api/models.py`
defines the audit schema (suppliers, parcels, runs, run_steps, evidence, verdicts, dds_documents)
with the Alembic async environment in `apps/api/alembic/`; `db.py` builds the async engine in the
lifespan-owned `services.py` (`AppServices`), which also constructs the shared cache and GFW/FIRMS
clients and the `RunManager`. `store.py` is the deep persistence module; `runner.py` executes
scenarios through `RunOrchestrator` (with the new `on_step` hook and injectable `run_id`) and batch
records through the deterministic assess + `code_checks` path under an asyncio semaphore, persisting
and broadcasting steps live. Routers cover suppliers, runs (start/detail/evidence/stream/decision),
batch (start/summary/records/stream), DDS JSON/XML, and a mock SAP vendor store. SSE uses
`sse-starlette` (`snapshot`/`step`/`state`/`progress`/`done`). `pnpm gen:api` regenerated the
contract; the Effect client validates every payload with `Schema` and exposes REST methods plus
`streamRun`/`streamBatchRun` over `Stream` + `Sse`. API tests run on SQLite via `aiosqlite` with
respx-mocked sources; CI adds a Postgres service job that runs `alembic upgrade head` and
`alembic check`. Live end-to-end still needs the §3 keys and Bedrock model access.
Follow-up (2026-09-11): the runtime client now uses adaptive retries and explicit timeouts, prompt
caching is an opt-in `BEDROCK_PROMPT_CACHE` knob (off by default, below current prompt thresholds),
and `python -m terrasentry_core.agents.preflight` pings both roles and serves the §3 gate.
126 Python tests green.

M5 landed (2026-09-11): the cockpit. `apps/web/src/routes/` now has the dashboard, suppliers
list/detail, runs list/detail, and batch list/detail behind a sidebar shell
(`components/layout/app-sidebar.tsx`), with filters and tabs synced to URL search params. Data access
stays Effect-first: `features/*/queries.ts` wrap the generated `ApiClient` in TanStack Query
`queryOptions`, mutations wrap `createRun`/`createBatchRun`/`submitDecision`, and
`features/runs/use-run-stream.ts` runs the SSE `Stream` through the shared `ManagedRuntime`
(`Stream.takeUntil` on a terminal state, step dedupe by `step_id`, query invalidation on close).
`components/data-table.tsx` wraps TanStack Table v9 (`useTable` + explicit `tableFeatures`), and
`components/map/map-view.tsx` loads MapLibre dynamically with `VITE_MAP_STYLE_URL` when set and a
no-tiles dark style otherwise. HITL is drivable from the UI (`ReviewDialog` approve/override with a
reviewer note). Prerequisites fixed while landing M5: successful scenario runs publish the terminal
`done` SSE frame, `ApiClientError` carries the HTTP status for typed 404/409/422 states,
`decodeRunEventStream` is part of the package barrel, and `GET /health` reports `{ offline,
fixtures_loaded }` for the rehearsal badge. The api-client schemas are plain `Schema.Struct`s rather
than `Schema.Class`es because TanStack Start serializes loader/query data with Seroval, which
rejects class instances; SSR of every cockpit route is serialization-error-free. Offline rehearsal
support lives in `terrasentry_integrations.fixtures` (`export`/`prime` CLI over the cache),
`CACHE_OFFLINE` and `CACHE_FIXTURES_DIR` settings, and API startup priming. `apps/web` gained a
vitest + happy-dom suite (26 tests); `pnpm check`, `pnpm test`, and the Nitro build are green.
Live-key end-to-end still needs the §3 gates; fixture files are recorded at the M6 rehearsal because
FIRMS cache windows are date-relative.

---

## 12. Open questions to revisit

| Question | Impact | Trigger to revisit |
| --- | --- | --- |
| SAP access: API Hub sandbox or BTP trial obtained? | `SAP_MODE=sandbox` vs `stub` | Day 1 access check |
| Which Bedrock region and model IDs are enabled? | Config only | Day 1 model-access check |
| Does GFW expose a bulk/async query endpoint for 50 polygons? | Batch wall-clock time | First 10-record pipeline run |
| Is Neon acceptable for team dev, or install Postgres? | Dev setup friction | Team environment review |
| Is AgentCore Runtime worth the deploy time for judging? | Stretch scope | After the batch rehearsal passes |
| TanStack Query vs `@effect/atom-react` for client state | Cockpit state architecture | Only if client state outgrows server-cache patterns |
| Add durable HITL (LangGraph-style interrupts)? | Approval UX | Only if in-process review proves insufficient |
